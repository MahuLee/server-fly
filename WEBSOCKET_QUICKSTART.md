# WebSocket 实时推送 - 快速开始指南

## 🚀 5分钟快速验证

按照以下步骤快速验证 WebSocket 实时状态推送功能。

---

## 📋 前置条件

- ✅ Node.js 已安装
- ✅ 后端依赖已安装 (`cd backend && npm install`)
- ✅ 前端依赖已安装 (`cd frontend && npm install`)

---

## 🎯 步骤 1: 启动后端服务

```bash
cd backend
npm start
```

**预期输出：**
```
Database initialized
Action executor initialized
WebSocket service started
Health check scheduler started
Server running on port 3001
```

✅ 看到 "WebSocket service started" 表示 WebSocket 服务已启动

---

## 🎯 步骤 2: 启动前端应用

打开新的终端窗口：

```bash
cd frontend
npm start
```

浏览器会自动打开 http://localhost:3000

---

## 🎯 步骤 3: 创建测试环境和节点

### 3.1 创建环境

1. 在前端界面，点击左侧"环境选择器"
2. 点击"新建环境"
3. 输入：
   - 名称：`测试环境`
   - 描述：`WebSocket 测试`
4. 点击"创建"

### 3.2 添加服务器节点

1. 确保在"服务架构"标签页
2. 从左侧节点库拖拽"服务器"节点到画布
3. 双击节点，配置：
   - 名称：`测试服务器`
   - IP：`192.168.1.100`
   - 端口：`22`
   - 用户名：`admin`（可选）
   - 密码：`test123`（可选）

### 3.3 配置健康检查

1. 在节点属性抽屉中，切换到"健康检查"标签
2. 配置：
   - 检查类型：`TCP`
   - 检查间隔：`10` 秒（测试用，生产环境建议30秒）
   - 超时时间：`5` 秒
3. 点击"保存"

### 3.4 保存架构图

点击顶部工具栏的"保存"按钮

---

## 🎯 步骤 4: 切换到监控一览

1. 点击顶部导航栏的"监控一览"标签
2. 左侧选择"测试环境"
3. 观察右上角的连接状态

**预期效果：**
- ✅ 连接状态显示：绿色圆点 + "实时监控中"
- ✅ 画布中显示刚才创建的节点
- ✅ 节点初始状态为灰色（未知）

---

## 🎯 步骤 5: 观察实时状态更新

### 5.1 等待健康检查执行

由于配置的检查间隔是10秒，等待约10-15秒后：

**预期效果：**
- 节点状态会自动变化
- 如果 IP 可达：节点变为绿色（正常）
- 如果 IP 不可达：节点变为红色（异常）

### 5.2 查看浏览器控制台

按 F12 打开开发者工具，在 Console 中应该看到：

```
[MonitoringView] Received state update for node <nodeId>: {status: "running", ...}
```

### 5.3 查看 WebSocket 消息

1. 在开发者工具中，切换到 "Network" 标签
2. 点击 "WS" 过滤器
3. 点击 WebSocket 连接
4. 切换到 "Messages" 标签

**预期看到的消息：**
```json
{
  "type": "state_update",
  "envId": "...",
  "nodeId": "...",
  "data": {
    "status": "running",
    "lastCheckTime": "2026-01-21T10:30:00Z",
    "message": "服务运行正常"
  },
  "timestamp": "2026-01-21T10:30:00Z"
}
```

---

## 🎯 步骤 6: 验证后端日志

在后端终端中，应该看到类似的日志：

```
Client client_xxx connected to environment <envId>
Broadcasted state_update to 1 clients in environment <envId>
```

---

## ✅ 验证清单

完成以下检查，确认功能正常：

- [ ] 后端服务启动成功，WebSocket 服务已启动
- [ ] 前端应用正常运行
- [ ] 创建了测试环境和节点
- [ ] 配置了健康检查（间隔10秒）
- [ ] 保存了架构图
- [ ] 切换到监控一览界面
- [ ] 连接状态显示"实时监控中"（绿色圆点）
- [ ] 节点状态会自动更新（10秒后）
- [ ] 浏览器控制台显示状态更新日志
- [ ] Network 标签中看到 WebSocket 消息
- [ ] 后端日志显示广播消息

---

## 🎨 状态颜色说明

| 状态 | 颜色 | 含义 |
|------|------|------|
| 🟢 绿色 | running | 服务正常运行 |
| 🔴 红色 | error | 服务异常或无法连接 |
| 🟡 黄色 | warning | 服务有警告 |
| ⚪ 灰色 | unknown | 状态未知 |

---

## 🐛 常见问题

### Q1: 连接状态显示"连接已断开"

**解决方法：**
1. 检查后端服务是否运行
2. 检查浏览器控制台是否有错误
3. 确认端口 3001 没有被占用

### Q2: 节点状态不更新

**解决方法：**
1. 确认已配置健康检查
2. 等待足够的时间（至少一个检查间隔）
3. 查看后端日志，确认健康检查正在执行

### Q3: 浏览器控制台报 WebSocket 错误

**解决方法：**
```bash
# 检查后端服务
curl http://localhost:3001/health

# 检查 WebSocket 状态
curl http://localhost:3001/api/websocket/status
```

---

## 🧪 高级测试

### 测试多个节点

1. 添加更多节点（服务、数据库、缓存等）
2. 为每个节点配置健康检查
3. 观察多个节点的状态同时更新

### 测试断线重连

1. 停止后端服务
2. 观察连接状态变为"连接已断开"
3. 重启后端服务
4. 观察自动重连（最多3次尝试）

### 测试多环境切换

1. 创建多个环境
2. 在监控一览中切换环境
3. 观察 WebSocket 连接切换

---

## 📊 性能测试

### 测试脚本

```bash
cd backend
node test-websocket-push.js
```

这个脚本会：
- 检查环境和节点配置
- 验证 WebSocket 服务状态
- 提供详细的测试指导

---

## 🎓 下一步

完成快速验证后，你可以：

1. **阅读详细文档**
   - `WEBSOCKET_REALTIME_PUSH.md` - 完整功能说明
   - `ARCHITECTURE_API_UPDATE.md` - API 更新说明

2. **配置生产环境**
   - 调整健康检查间隔（建议30-60秒）
   - 配置更多节点类型
   - 设置合理的超时时间

3. **监控和优化**
   - 查看 WebSocket 连接数
   - 监控消息延迟
   - 优化健康检查配置

---

## 💡 提示

- 健康检查间隔不要设置太短，避免频繁检查
- 生产环境建议间隔 30-60 秒
- 测试时可以设置 10 秒，方便快速验证
- 注意观察浏览器控制台和后端日志

---

## 📞 需要帮助？

如果遇到问题：

1. 查看 `WEBSOCKET_REALTIME_PUSH.md` 的故障排查部分
2. 检查浏览器控制台和后端日志
3. 运行测试脚本获取诊断信息

---

**祝你测试顺利！** 🎉

---

**更新日期**: 2026-01-21  
**版本**: 1.0.0
